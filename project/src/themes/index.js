import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
   palette: {
        primary: {
            main: '#8ED73D'
        },
        secondary: {
            light: '#FFB08D',
            main: '#FF9567',
            dark: '#FF5B14'
        }
   } 
}); 

export default theme;